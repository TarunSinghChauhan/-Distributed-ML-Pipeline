### Resume Impact Bullets - Data Engineer

- **Architected and implemented** a distributed text processing pipeline using **Apache Spark** and **Airflow** to handle 5M+ documents (50GB+), improving ML training data ingestion throughput by **40%** through parallel partition processing.
- **Developed** a near-duplicate detection system using **MinHash LSH** in **PySpark**, reducing dataset redundancy by **18%** while maintaining a Jaccard similarity threshold of 0.8, significantly decreasing downstream LLM training costs.
- **Engineered** a multi-stage data quality framework incorporating **Great Expectations** and **Pandera**, implementing perplexity-based filtering and toxicity detection that improved dataset signal-to-noise ratio by **25%** across 4 distinct data sources.
- **Designed** an immutable dataset versioning system within an **S3-compatible Lakehouse** (MinIO), automating the generation of SHA256 deterministic manifests and **Streamlit** dashboards for real-time pipeline monitoring and data drift analysis.
